# Check: Add HEALTHCHECK instruction in your Dockerfile
# Healthchecks are handled externally
DS026
# Check: HTTP request smuggling via llhttp HTTP request parser
#        https://avd.aquasec.com/nvd/cve-2023-37276
#
# This vulnerability only affects users of aiohttp as an HTTP server
# (ie aiohttp.Application), you are not affected by this vulnerability if you
# are using aiohttp as an HTTP client library (ie aiohttp.ClientSession).
CVE-2023-37276
# When installing a package from a Mercurial VCS URL
# https://avd.aquasec.com/nvd/cve-2023-5752
CVE-2023-5752
